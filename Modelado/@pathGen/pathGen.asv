classdef pathGen
  properties
    coefs;
	points;
    currPoint;
  end
  methods
    function this = pathGen()
        this.currPoint = thispoints(1);
    end
    this = generatePath(this, points, type);
    obj = getObj(coefs,currPos,speed);
    plotPath(this, fig, axes);
  end
end
